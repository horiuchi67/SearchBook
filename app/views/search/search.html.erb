<div class ="body">
	<div class ="container">
		<div class="row">
      <div class="col-xs-9">
        <% if @model == "user" %>
          <p class="h2">Users search for '<%= @word %>'</p>
          <table class="table">
            <thead>
              <tr>
                <th>image</th>
                <th>name</th>
                <th colspan="3"></th>
              </tr>
            </thead>
            <% @posts.each do |post| %>
              <tbody>
                <tr>
                  <td><%= attachment_image_tag post, :profile_image, :fill, 20, 20, format: 'jpeg', class: " ", fallback: "no_image.jpg" ,size:'20x20' %>
                  </td>
        				  <td>
        				    <%= link_to post.name, post %>
        				  </td>
                  <td><%= link_to "Show", user_path(post) %><td>
                </tr>
              </tbody>
            <% end %>
          </table>
        <% else %>
          <p class="h2">Books search for '<%= @word %>'</p>
          <table class="table">
            <thead>
              <tr>
                <th>title</th>
                <th>opinion</th>
                <th colspan="3"></th>
              </tr>
            </thead>
            <% @posts.each do |post| %>
              <tbody>
                <tr>
                  <td><%= link_to post.title, post %>
                  </td>
                  <td>
                    <%= post.body  %>
                  </td>
                </tr>
              </tbody>
            <% end %>
          </table>
        <% end %>
      </div>
    </div>
  </div>
</div>